= semantic_form_for :offer, :url => offers_path, :method => :get do |f|
  = f.inputs :name => "Form options" do
    = f.input :uid
    = f.input :pub0
    = f.input :page
    = f.actions do
      = f.action :submit, :label => "Submit"
